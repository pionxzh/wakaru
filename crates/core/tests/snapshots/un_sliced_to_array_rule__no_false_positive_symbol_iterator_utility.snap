---
source: crates/core/tests/un_sliced_to_array_rule.rs
expression: output
---
function maybeIter(arr, count) {
    if (Symbol.iterator in Object(arr)) {
        return take(arr, count);
    }
    return arr.slice(0, count);
}
const x = maybeIter(items, 2);
